import { Text, View, StyleSheet } from "react-native";
const MovieDetails = () => {
  return (
    <View className=''>
      <Text className='text-5xl text-primary font-bold'>Onboarding</Text>
    </View>
  );
};

export default MovieDetails;
const styles = StyleSheet.create({});
