import { Text, View, StyleSheet } from "react-native";
const Search = () => {
  return (
    <View className=''>
      <Text className='text-5xl text-primary font-bold'>Search</Text>
    </View>
  );
};

export default Search;
const styles = StyleSheet.create({});
