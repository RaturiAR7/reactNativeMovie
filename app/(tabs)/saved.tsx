import { Text, View, StyleSheet } from "react-native";
const Saved = () => {
  return (
    <View className=''>
      <Text className='text-5xl text-primary font-bold'>Saved</Text>
    </View>
  );
};

export default Saved;
const styles = StyleSheet.create({});
